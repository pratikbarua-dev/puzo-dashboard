# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# serwist / pwa build output
public/sw.js
public/sw.js.map
public/workbox-*
public/fallback-*

# agent tool state. `.claude/worktrees/` holds full checkouts of this repo,
# each with its own .git and .next -- several hundred MB that must never be
# committed into the repo they were cut from.
.claude/
